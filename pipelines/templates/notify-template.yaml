parameters:
  - name: 'webhook_url'
    type: string

steps:
  - task: CmdLine@2
    inputs:
      script: |
        chmod +x "$(Build.SourcesDirectory)/pipelines/templates/scripts/notify.sh"
  - bash: "$(Build.SourcesDirectory)/pipelines/templates/scripts/notify.sh"
    displayName: 'Sending Notification'
